[project]
name = "pywebview-react-template"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.9"
dependencies = [
    "pywebview>=5.0",
]

[dependency-groups]
dev = [
    "nuitka[onefile]>=4.0.1",
]
